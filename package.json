{
  "name": "hvackeros-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "HVACKEROS website: landing, about, members and news — static, built with Vite, deployed to GitHub Pages.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "dev:host": "vite --host",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "0.169.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
